# Hermes

Tienda con HTML + CSS + JavaScript vanilla sobre Vite, con estilos en Tailwind.

## Desarrollo

1. `npm install`
2. `npm run dev`

## Datos

El catálogo se carga desde `public/data/products.json`.
